# Cross-Check Report: u6 infra/CI

**Scope**: Unit `u6 infra/CI` (GitHub Actions workflows + MkDocs/GitHub Pages site + operator runbook)
**Date**: 2026-05-03
**Checked by**: Codex
**Triggered by**: `dev-investo` health check after all construction stages completed

---

## Inputs

| Document | Role |
|----------|------|
| `docs/requirements.md` | Single-source-of-truth FR/NFR list |
| `aidlc-docs/inception/user-stories/stories.md` | US-003 / US-005 / US-007 acceptance criteria |
| `aidlc-docs/inception/application-design/unit-of-work-story-map.md` | Story-to-unit ownership |
| `aidlc-docs/construction/plans/u6-infra-ci-code-generation-plan.md` | u6 implementation plan |
| `aidlc-docs/construction/u6-infra-ci/code/summary.md` | Code-generation closeout and traceability |
| Implementation: `.github/workflows/daily-briefing.yml`, `.github/workflows/pages.yml`, `mkdocs.yml`, `site_docs/`, `archive/`, `CONTRIBUTING.md`, `src/investo/__main__.py`, `tests/unit/orchestrator/test_main.py` | Artifacts verified |

---

## Scope Filter

Per `unit-of-work-story-map.md`, **u6 infra/CI** is responsible for:

- **Primary infrastructure closure**: GitHub Actions cron, workflow_dispatch, GitHub Pages build/deploy, secret injection
- **Stories closed at infra level**: US-003 static publishing, US-005 scheduled execution
- **Touched story**: US-007 via GitHub Actions failure signaling and operator runbook
- **FRs**: FR-003 Pages/static-site slice, FR-005 workflow slice, FR-007 GHA default notification slice
- **NFRs covered whole or share**: NFR-001 workflow timeout, NFR-002 zero-cost infra/deps, NFR-004 site/runbook disclaimer visibility, NFR-007 GitHub Secrets wiring

Out of scope: runtime stage routing and Telegram delivery retry policy. u5 owns runtime routing and closed the original FR-007 alert-retry mismatch in the 2026-05-04 follow-up fix.

---

## Summary

| Status | Count | Percentage |
|--------|------:|-----------:|
| ✅ Complete | 10 | 91% |
| ⚠️ Partial | 1 | 9% |
| ❌ Gap | 0 | 0% |
| 🔄 Deferred | 0 | 0% |
| ⏳ In Progress | 0 | 0% |
| **Total** | **11** | **100%** |

**Overall compliance for u6 infra/CI**: u6's infrastructure contract is complete. The one Partial is operational: first real cron, first real Pages deploy, and real Telegram bot/channel handshake can only be verified after secrets are configured in GitHub.

---

## Functional Requirements

| ID | Description | Status | Evidence | Notes |
|----|-------------|--------|----------|-------|
| FR-003 | GitHub Pages static publishing | ✅ Complete | `.github/workflows/pages.yml`, `mkdocs.yml`, `site_docs/archive` symlink, `mkdocs build --strict` | Pushes touching archive/site files build and deploy the MkDocs site; failed build blocks deploy and preserves prior site. |
| FR-005 | GitHub Actions scheduled execution | ✅ Complete | `.github/workflows/daily-briefing.yml`, `INVESTO_TARGET_DATE` handling in `__main__.py` | Cron, workflow_dispatch, secret injection, timeout, concurrency, and `python -m investo` runner are wired. |
| FR-007 | GHA default failure notification fallback | ✅ Complete | `daily-briefing.yml`, u5 exit-code mapping, runbook | Pipeline `FAILED` returns exit code 1, making the workflow red; GitHub Actions default failure notification can then fire. Alert retry is implemented and verified in u5 scope. |

### FR-003 Acceptance-Criterion Detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| Markdown file is published as a static site | ✅ | `pages.yml` runs `mkdocs build --strict`; `mkdocs.yml` uses `site_docs` |
| GitHub Pages deployment | ✅ | `actions/configure-pages`, `actions/upload-pages-artifact`, `actions/deploy-pages@v4` |
| Date/archive navigation | ✅ | `site_docs/archive -> ../archive` tracked symlink; `archive/index.md`; MkDocs nav |
| Search or date/year exploration | ✅ | MkDocs Material search plugin with `ko`/`en`; archive hierarchy rendered from markdown paths |

### FR-005 Acceptance-Criterion Detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| GitHub Actions `schedule` cron trigger | ✅ | `daily-briefing.yml` `on.schedule` |
| Weekday KST 07:00 / UTC Sun-Thu 22:00 | ✅ | `0 22 * * 0,1,2,3,4` |
| Saturday KST 09:00 / UTC Sat 00:00 | ✅ | `0 0 * * 6` |
| `workflow_dispatch` manual trigger | ✅ | `workflow_dispatch.inputs.target_date`; `INVESTO_TARGET_DATE` env |
| Single job runtime cap | ✅ | `timeout-minutes: 12` on daily briefing job |
| Execution timing visible in logs | ✅ | u5 `PipelineResult.stage_timings` + stdlib logs; workflow preserves logs |

### FR-007 Acceptance-Criterion Detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| GitHub Actions default failure notification | ✅ | u5 maps `FAILED` to exit code 1; `daily-briefing.yml` runs that command directly |
| Failure alert uses operator chat, not public channel | ✅ | Separate secrets wired; u5 validates chat-ID disjointness before dispatcher construction |
| Alert-send failure visible in GHA logs | ✅ | u5 logs warning on alert delivery failure; workflow keeps command logs |
| Alert-send retry | ✅ | u5 `_safe_alert` / `_attempt_boot_alert` retry alert delivery; not an infra/YAML behavior |

---

## User Stories

| ID | Title | Status | Evidence | Notes |
|----|-------|--------|----------|-------|
| US-003 | GitHub Pages에 시황을 정적 게시한다 | ✅ Complete | `pages.yml`, `mkdocs.yml`, `site_docs/`, tracked archive symlink | u3 writes archive markdown; u6 builds and deploys it. |
| US-005 | GitHub Actions cron으로 자동 실행된다 | ✅ Complete | `daily-briefing.yml`, `CONTRIBUTING.md` runbook | Cron schedule, manual trigger, timeout, and secrets are documented and wired. |
| US-007 | 파이프라인 실패 시 운영자 1:1 chat으로 알림 받는다 | ✅ Complete | GHA failure fallback + runbook + u5 alert retry | u6 closes GHA fallback; u5 closes runtime alert routing, chat isolation, and alert-delivery retry. |

---

## Non-Functional Requirements

| ID | Description | Status | Evidence | Notes |
|----|-------------|--------|----------|-------|
| NFR-001 | GitHub Actions run bounded | ✅ Complete | `timeout-minutes: 12` | 10-minute design budget plus 2-minute safety margin. |
| NFR-002 | Zero-cost infra and dependency split | ✅ Complete | GitHub Actions + Pages; `mkdocs-material` only in `docs` extra | No paid API dependency introduced by u6. |
| NFR-003 | Failure visibility | ✅ Complete | u5 exit codes + workflow command log + runbook | Runtime failure policy is u5; u6 preserves exit code and logs. |
| NFR-004 | Disclaimer visibility | ✅ Complete | `site_docs/index.md`, `site_docs/about.md`, u3 publish guard | Site pages carry disclaimer; daily briefing markdown remains guarded by u3. |
| NFR-007 | GitHub Secrets / no secret echo | ✅ Complete | `daily-briefing.yml` env block; grep check | Secrets are injected via `${{ secrets.* }}` and not echoed or debug-printed. |
| Maintainability / operator handoff | ✅ Complete | `CONTRIBUTING.md` operator runbook | Secrets, cron schedule, manual trigger, holiday recovery, and Pages deploy are documented. |

---

## Verification Run

| Command | Result |
|---------|--------|
| `uv run pytest tests/unit/orchestrator/test_main.py tests/integration/test_pipeline.py -q` | ✅ 52 passed |
| `uv run mkdocs build --strict` | ✅ built in 0.27s; Material-for-MkDocs informational warning only |
| `uv run ruff check src/investo/__main__.py tests/unit/orchestrator/test_main.py` | ✅ all checks passed |
| `uv run mypy --strict src/investo/__main__.py` | ✅ no issues in 1 source file |
| `uv run python -c "... yaml.safe_load(...)"` for both workflows and `mkdocs.yml` | ✅ YAML parsed |
| `git ls-files -s site_docs/archive ...` | ✅ `site_docs/archive` tracked as symlink mode `120000`; site/workflow files tracked |
| `rg` for secret echo/debug and forbidden dependency names | ✅ expected docs/env names only; no `echo secrets`, `set -x`, or debug flag |

Note: an attempted `ruff check` over YAML/Markdown files was intentionally discarded because Ruff parses Python, not GitHub Actions YAML or MkDocs YAML.

---

## Gaps Analysis

### GAP-001: First production run is operational verification, not local verification

**Requirement**: The actual cron fire, Pages deployment, and Telegram delivery must work in GitHub after secrets are configured.

**Status**: ⚠️ Partial at operational level. Local verification confirms workflow syntax, command wiring, strict MkDocs build, entrypoint override tests, and tracked symlink presence. The real hosted environment depends on repository settings and secrets.

**Impact**: Low for u6 stage gate. This is the normal final operational check for GitHub Actions / Pages.

**Proposed Action**: After commit/push, verify the first `workflow_dispatch` run in GitHub Actions, check Pages deployment URL, and confirm Telegram channel/operator chat delivery with real secrets.

### Linked Existing Gap: FR-007 alert retry in u5

Resolved on 2026-05-04. The u5 follow-up fix retries operator-alert delivery failures and logs a warning only after final failure. u6 continues to own only the GitHub Actions fallback side by preserving exit code 1 and logs.

---

## TECH-DEBT in u6 Scope

| ID | Priority | Description | Cross-check status |
|----|----------|-------------|--------------------|
| DEBT-022 | Low | `pages.yml` permissions are workflow-level instead of job-level | Resolved 2026-05-03 |
| DEBT-023 | Low | `daily-briefing.yml` installs `--extra dev` but never runs pytest | Resolved 2026-05-03 |
| DEBT-024 | Low | `astral-sh/setup-uv@v3` is not pinned to SHA | Resolved 2026-05-04 |
| DEBT-025 | Low | `ConfigError.missing_vars` overloaded for malformed `INVESTO_TARGET_DATE` | Resolved 2026-05-03 |
| DEBT-026 | Low | `archive/.gitkeep` redundant alongside `archive/index.md` | Resolved 2026-05-03 |
| DEBT-027 | Low | Windows checkout symlink limitation undocumented | Resolved 2026-05-04 |

No u6-scoped TECH-DEBT items remain active after the 2026-05-03/2026-05-04 cleanup passes. No new TECH-DEBT items were added by this cross-check.

---

## Sign-Off

✅ **u6 infra/CI cross-check PASSED with one expected operational Partial.**

The unit is complete for its contract: scheduled workflow, manual trigger, secret wiring, runtime timeout, Pages workflow, MkDocs strict build, tracked archive symlink, and operator runbook. With u6 checked and the u5 alert-retry follow-up resolved, all completed construction units now have cross-check reports without known local implementation gaps.
