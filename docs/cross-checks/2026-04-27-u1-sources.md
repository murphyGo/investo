# Cross-Check Report: u1 sources

**Scope**: Unit `u1 sources` (Source Adapters layer)
**Date**: 2026-04-27
**Checked by**: Claude
**Triggered by**: Unit-completion auto-action after u1 sources Code Generation Step 10 closeout

> **2026-05-04 status update**: This report is a historical cross-check
> from the initial one-adapter u1 slice. The documented FR-001 / US-001
> category gap was closed by the later u1 source extensions: the current
> implementation has 11 registered adapters and covers all 5 current
> `Category` enum values (calendar, price, macro, news, earnings). See
> `aidlc-docs/aidlc-state.md` and the u1 extension closeout summaries
> for the current source inventory.

---

## Inputs

| Document | Role |
|----------|------|
| `docs/requirements.md` | Single-source-of-truth FR/NFR list |
| `aidlc-docs/inception/user-stories/stories.md` | INVEST stories with ACs |
| `aidlc-docs/inception/application-design/unit-of-work-story-map.md` | Story↔Unit mapping (which stories u1 owns) |
| `aidlc-docs/construction/u1-sources/functional-design/` | Functional Design artifacts |
| `aidlc-docs/construction/u1-sources/nfr-requirements/nfr-requirements.md` | 30 NFR ACs |
| `aidlc-docs/construction/u1-sources/code/summary.md` | Step 10 closeout summary with AC traceability |
| Implementation: `src/investo/sources/`, `tests/unit/sources/`, `scripts/check_no_paid_apis.py`, `CONTRIBUTING.md` | The artifacts being verified |

---

## Scope filter

Per `unit-of-work-story-map.md`, **u1 sources** is responsible for:

- **Primary stories**: US-001 (자동 시장 데이터 수집), US-008 (단일 모듈 추가로 새 데이터 소스 통합)
- **Touched-by**: US-009 (free-API constraint — u1 owns the adapter side; u2 owns the LLM side)
- **FRs**: FR-001 (데이터 수집)
- **NFRs covered (whole or share)**: NFR-001 share (≤ 4 min collect budget), NFR-002 (free APIs only — u1 slice), NFR-003 (graceful degradation), NFR-005 (plugin maintainability), NFR-006 (PBT partial), NFR-007 (XML safety / URL guard / no eval/pickle/exec)

NFR-004 (disclaimer enforcement) and NFR-001 overall budget are owned by u2 / u5 respectively and are **out of scope** for this cross-check.

---

## Summary

| Status | Count | Percentage |
|--------|------:|-----------:|
| ✅ Complete | 11 | 79% |
| ⚠️ Partial | 1 | 7% |
| ❌ Gap | 0 | 0% |
| 🔄 Deferred | 2 | 14% |
| ⏳ In Progress | 0 | 0% |
| **Total** | **14** | **100%** |

**Overall compliance for u1 sources at initial closeout**: 100% of u1's design contract was fulfilled. The original ⚠️ Partial (FR-001 AC-2) reflected MVP scope at the time: u1 initially shipped the framework + 1 reference adapter. **Current status**: the follow-up source extensions closed that category gap.

---

## Functional Requirements (in u1's scope)

| ID | Description | Status | Evidence | Notes |
|----|-------------|--------|----------|-------|
| FR-001 | 데이터 수집 | ✅ Complete after extensions | `src/investo/sources/aggregator.py`, registered source adapters, source tests | Initial framework was complete with `fomc-rss`; later u1 extensions added price, crypto, macro, news, and earnings coverage. See GAP-001 status update. |

### FR-001 acceptance-criterion detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 무료 API/RSS만 사용 (월 $0) | ✅ | `scripts/check_no_paid_apis.py` + `tests/unit/sources/test_no_paid_apis.py` (4 tests covering subprocess + populated-blocklist detection) |
| 소스 카테고리 1개 이상 | ✅ | Initially only "calendar" via `fomc_rss.py`; later u1 extensions added the remaining current categories. |
| 신규 소스 추가/제거가 코드 변경 1곳 (plugin/registry) | ✅ | `CONTRIBUTING.md` 4-step procedure + `__init__.py` docstring + 7 plugin-contract tests (`test_plugin_contract.py`) + drift guard (`EXPECTED_ADAPTER_COUNT`) |
| 단일 소스 실패 시 graceful degradation | ✅ | `test_aggregator.py` covers AC-3.1–3.5 (NFR-003) — `SourceFetchError` → log + `[]`, programmer errors propagate |

---

## Non-Functional Requirements (in u1's scope)

| ID | Description | Status | Evidence | Notes |
|----|-------------|--------|----------|-------|
| NFR-001 (share) | u1 collect ≤ 4 min (≤ 70 s with one slow + 2 fast) | ✅ | `test_fetch_all_budget.py::test_fetch_all_within_budget_with_one_slow_adapter` (scaled 100x) + `test_fetch_all_dispatches_adapters_concurrently` | Concurrency proof + per-adapter 60-s outer cap from `_retry.py` |
| NFR-002 (u1 slice) | Free APIs only | ✅ | `scripts/check_no_paid_apis.py` (CI guard) + `test_no_paid_apis.py` (subprocess + monkeypatched detection) + FOMC URL is free-tier without auth | u2's Claude Code CLI slice is out of scope for u1 cross-check |
| NFR-003 | Graceful degradation | ✅ | `test_aggregator.py` AC-3.1–3.5 (does-not-raise on `SourceFetchError`, isolation, all-fail returns `[]`) + programmer-error propagation tests |
| NFR-005 | Plugin maintainability | ✅ | `test_plugin_contract.py` (drift guard, +1 stub, duplicate-name, `__all__` lock, re-export identity); 4-step procedure documented in `CONTRIBUTING.md` + `__init__.py` docstring |
| NFR-006 | PBT partial | ✅ | `test_window.py` (AC-6.1 + AC-6.2 PBT × 100 examples each) + `test_retry.py` (AC-6.3 PBT × 100 examples × 2 properties) — total 4 PBT tests |
| NFR-007 (u1 slice) | XML safety + URL guard + no eval/pickle/exec | ✅ | AC-7.1 (5MB cap, post-hoc — DEBT-003 tracks streaming variant), AC-7.2 (HTML strip), AC-7.3 (URL scheme guard), AC-7.4 (tz-aware `published_at`), AC-7.5 (passive — no such calls), AC-7.6 (`test_xml_safety.py` regex + positive defusedxml guard) |

### NFR AC-to-test traceability

All 30 ACs from `aidlc-docs/construction/u1-sources/nfr-requirements/nfr-requirements.md` are pinned by tests or documented passive guarantees. See `aidlc-docs/construction/u1-sources/code/summary.md` for the complete traceability table.

---

## User Stories (in u1's scope)

| ID | Title | Status | Evidence | Notes |
|----|-------|--------|----------|-------|
| US-001 | 매일 시장 데이터를 자동 수집한다 | ✅ Complete after extensions | Same as FR-001 above | Initial partial closed once follow-up source adapters landed. |
| US-008 | 새 데이터 소스를 단일 모듈 추가로 통합한다 | ✅ Complete | 4-step procedure + `@register` + `SourceAdapter` Protocol + drift-guard tests + `CONTRIBUTING.md` | Closed per Step 9/10 |
| US-009 (u1 slice) | 운영비 월 $0 (free-tier 데이터 소스) | ✅ Complete | CI cost guard infrastructure in place; FOMC RSS is free | u2 owns the LLM-side slice (Claude Code CLI); not in u1 cross-check scope |

### US-001 acceptance-criterion detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 무료 API/RSS만 사용 | ✅ | CI cost guard + FOMC RSS endpoint |
| 카테고리별 소스 1개 이상 | ✅ | Follow-up source extensions added adapter coverage across the current category set |
| 신규 소스 = 단일 모듈 추가 | ✅ | US-008 closes this (4-step procedure) |
| 단일 소스 실패 시 graceful degradation | ✅ | NFR-003 tests + AC-3.1–3.5 |
| 응답 검증 (pydantic), 실패 시 항목 제외 | ✅ | `fomc_rss.py::_normalize_entry` catches `pydantic.ValidationError` → drop entry; sibling entries unaffected |

### US-008 acceptance-criterion detail

| Criterion | Status | Evidence |
|-----------|--------|----------|
| 새 소스 = (1) 모듈 파일 1개 + (2) registry 한 줄 등록 | ✅ | NFR AC-5.1 / 5.4; documented in `CONTRIBUTING.md` and `__init__.py` |
| 공통 인터페이스 (`fetch() -> list[NormalizedItem]`) | ✅ | `SourceAdapter` Protocol in `protocol.py` |
| timeout/retry 정책은 공통 베이스 | ✅ | `retry_get` + `RetryConfig` in `_retry.py`; AC-1.2 / AC-6.3 / AC-7.1 pinned |
| 단일 소스 실패는 전체를 죽이지 않음 | ✅ | NFR-003 tests |
| 신규 소스 추가 PR 예시·체크리스트 | ✅ | `CONTRIBUTING.md` "Adding a new data source" + "PR description checklist" |

---

## Construction-stage coverage (per AIDLC plan)

| Stage | Per execution-plan.md | Actual | Verified by |
|-------|-----------------------|--------|-------------|
| Functional Design | EXECUTE | ✅ Complete (3 artifacts: domain-entities, business-logic-model, business-rules) | `aidlc-docs/construction/u1-sources/functional-design/` populated |
| NFR Requirements | EXECUTE | ✅ Complete (30 ACs documented) | `aidlc-docs/construction/u1-sources/nfr-requirements/nfr-requirements.md` |
| NFR Design | SKIP | 🔄 Skipped per plan | execution-plan.md Step §3 "NFR Requirements 수준에서 흡수" |
| Infrastructure Design | SKIP | 🔄 Skipped per plan | execution-plan.md "GitHub Actions YAML이 design 자체" — owned by u6 |
| Code Generation | EXECUTE | ✅ Complete (10/10 steps) | `u1-sources-code-generation-plan.md` all `[x]`; `code/summary.md` written |
| Build and Test | DEFERRED (global, post-CG) | ⏳ Awaits other units' CG completion | Will run once after u2/u4/u5/u6 CG land |

---

## Files audit (vs `code/summary.md`)

### Source code (`src/investo/sources/`)
- ✅ All 8 files present matching the summary's table:
  `__init__.py` (37 LOC), `_window.py` (112), `_retry.py` (271), `_sanitize.py` (48), `protocol.py` (85), `_registry.py` (71), `aggregator.py` (79), `fomc_rss.py` (148)
- **Total**: 851 LOC

### Tests (`tests/unit/sources/`)
- ✅ All 12 test files present matching the summary's table
- ✅ Test count: 151 u1 tests pass (101 models tests run separately for the foundation; combined repo-wide pytest is 252)
- ✅ `conftest.py` extracts the autouse `_isolate_registry` fixture (Step 7)
- ✅ Recorded fixture `tests/unit/sources/fixtures/api/fomc-rss/feed.xml` (14 KB, RSS 2.0) + `meta.json`

### Other artifacts
- ✅ `scripts/check_no_paid_apis.py` (Step 10)
- ✅ `CONTRIBUTING.md` at repo root
- ✅ `aidlc-docs/construction/u1-sources/code/summary.md` (Step 10 closeout summary)

### FD-vs-implementation divergences (ratified in `aidlc-docs/audit.md`)
- ✅ Step 5 — `SourceAdapter.fetch(client, window)` instead of FD's `fetch(client, target_date)` (aggregator builds window once)
- ✅ Step 8 — FOMC feed is RSS 2.0, not Atom 1.0 as FD predicted (FD L6 corrected in same change)

Both deviations are minor refinements caught early; neither breaks any cross-unit contract. Audit-log entries serve as the canonical record.

---

## TECH-DEBT (carried forward)

Per `docs/TECH-DEBT.md`:

| ID | Priority | Origin | Description | u1 scope? |
|----|----------|--------|-------------|-----------|
| DEBT-001 | Medium | models step 3 | `Briefing` lacks `disclaimer ∈ rendered_markdown` invariant | Resolved 2026-05-03 |
| DEBT-002 | Medium | models step 3 | No date sanity bounds on `target_date` / `published_at` | Resolved 2026-05-03 |
| DEBT-003 | Low | u1 step 3 | `retry_get` 5 MB cap is post-hoc, not streaming | Resolved 2026-05-04 |
| DEBT-004 | Low | u1 step 4 | `_sanitize` depends on `bleach` (maintenance-mode); `nh3` recommended successor | Yes (u1) |
| DEBT-005 | Low | u1 step 7 | Aggregator log line is printf-style, not L5-structured | Resolved 2026-05-04 |

None block u1 stage close. Only DEBT-004 remains active today, and it is a watch item for a future bleach EOL / deprecation trigger.

---

## Gaps Analysis

### GAP-001: FR-001 AC-2 / US-001 AC-2 — source category coverage

**Requirement**: FR-001 lists 6 source categories — 주가/지수, 크립토 시세, 거시 지표(FRED 등), 연준 캘린더, 주요 기업 뉴스, 실적 캘린더 — and US-001 AC says "카테고리별 소스 1개 이상".
**Status**: ✅ Resolved by later u1 source extensions. At initial closeout, `fomc-rss` covered "연준 캘린더" only. Follow-up extensions added market price/index, crypto price, macro, news, and earnings adapters, bringing current coverage to all current `Category` enum values.
**Impact at initial closeout**: Medium. The plugin contract was complete, but the first slice was not yet useful enough for a full daily briefing.
**Why this was not a blocker for u1 stage close**: u1's initial design contract was "framework + 1 reference adapter", not "all categories". The Plan §0 explicitly scoped Step 8 to `fomc_rss.py` as the PoC. The later adapter extensions exercised the contract u1 locked in.

**Action Taken**: Added the missing adapter coverage as discrete u1 source extensions. The original suggested adapter names below are retained as historical planning context; the final implementation used the repo's current adapter set and naming.

| Suggested adapter | Category | Source | Free-tier confirmed |
|-------------------|----------|--------|--------------------|
| `coingecko.py` | crypto price | `https://api.coingecko.com/api/v3/...` | Yes (no key, ≤30 req/min free) |
| `yfinance_quote.py` | price/index | yfinance Python lib | Yes (unofficial; data via Yahoo public endpoints) |
| `fred_series.py` | macro | `https://api.stlouisfed.org/fred/...` | Yes (free key) — note: registers a key, must declare in PR per US-009 |
| `yfinance_earnings.py` | earnings | yfinance | Yes (same as quote) |
| `news_rss_aggregator.py` | news | aggregate Reuters/AP/SEC EDGAR RSS feeds | Yes (RSS, no auth) |

These were not part of the initial u1 stage; they were MVP-completing tasks for the development plan. They are now closed.

---

## Proposed Updates

### Add to development plan (post-u1)

| Gap | Requirement | Proposed task | Priority |
|-----|-------------|---------------|----------|
| GAP-001 | FR-001 AC-2 | Add source adapters for missing categories | ✅ Closed by later u1 source extensions |

These are NOT a u1 stage scope expansion — they are downstream PR tasks that exercise the now-locked plugin contract. Each follows the `CONTRIBUTING.md` 4-step procedure.

### Add to TECH-DEBT? — No

GAP-001 is a known scope decision (u1 = framework + 1 adapter), not technical debt. It belongs in the development plan / project backlog, not `docs/TECH-DEBT.md`.

### Cross-check report

Saved to `docs/cross-checks/2026-04-27-u1-sources.md` (this file).

---

## Critical gaps (high-priority)

**None for the u1 stage gate.** GAP-001 is a known MVP-completing task that can be tracked separately; the u1 unit's design contract is fulfilled.

---

## Recommendations

1. **Sign off u1 sources Code Generation as ✅ Complete** — the unit's contract is fulfilled; all 30 NFR ACs are pinned by tests; the 2 primary stories (US-001 partial, US-008 full) are at the expected u1-scope completion level.
2. **Add the 5 missing adapters as backlog tasks** before the project enters full Build and Test. Each is a single-file PR per `CONTRIBUTING.md`. Sequential dev effort is small (~1-2 hours each given the now-mature `retry_get` + `strip_html` + `defusedxml` toolchain).
3. **Proceed to u2 briefing Functional Design** — that unit consumes `fetch_all` + `NormalizedItem` (already published as the stable surface). u2 will define the LLM call contract, prompt structure, disclaimer enforcement (NFR-004), and 7-section schema.
4. **DEBT-001 (cross-unit)** — re-evaluate when u3 publisher's disclaimer-verification path lands. The model-level invariant strengthens defense-in-depth.
5. **DEBT-002 (cross-unit)** — re-evaluate at u5 orchestrator's `resolve_target_date` implementation. Date sanity bounds belong at the orchestrator boundary per the DEBT entry.

---

## Sign-off

✅ **u1 sources cross-check PASSED; GAP-001 later resolved by source extensions.**

Per dev-investo §6.4, the unit proceeded to later construction stages; all construction and cross-check work is now complete except operational live-run verification.
