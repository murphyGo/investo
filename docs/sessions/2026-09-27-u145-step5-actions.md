# u145 Step 5 — Actions execution and resource repair

## Authorization and pushed scope

The user replied `진행` to the explicit request to commit/push the reviewed isolated branch
and execute five sequential Actions probes. The 54-file scope matched the reviewed manifest.
Implementation commit `5e6566dd` and documentation whitespace correction `ab1c06c3` were pushed
to `codex/u145-resume-20260922`; the remote ref matched
`ab1c06c369033f070350424e2345fe124d4e8f32` exactly. The root worktree was untouched.

## Initial reference-run failure

[Run 36262389388](https://github.com/murphyGo/investo/actions/runs/36262389388) executed on
`ab1c06c369033f070350424e2345fe124d4e8f32` and failed the synthetic resource gate. Its aggregate
result was otherwise qualified: 11 x 10,000 rows, 22 requests, 326,233 bytes/response, 2,984 ms
CPU and 2,973 ms wall. Peak RSS increase was **288,579,584 bytes**, above the **268,435,456-byte**
ceiling. The live HF step was skipped; this execution counts as zero successful probes.

## Resource repair

The collector retained eleven complete 10,000-point OHLCV model histories even though the
metric layer uses only the latest 64 SPY observations. This violated the intended bounded
retention in Functional Design L4.6 and caused the Ubuntu RSS failure.

Every response still passes the complete decoder and all original-row validation. Before
storing each successful result, SPY is reduced to its last 64 observations. A sector retains
its intersection with those SPY dates plus its final two IEX points, at most 66. The final two
preserve parser success/minimum history and the latest endpoint when overlap is zero or one.
No independently truncated sector tail can evict a required SPY date.

The benchmark, 11 x 10,000-row input, interpreter baseline and all resource ceilings are
unchanged. No request, retry, secret, output or publication boundary changed.

## Repair validation

- Adapter, public metrics and probe suites: **152 passed in 32.54 seconds**.
- Added ten regression cases: eight full-history vs retained bundle/snapshot/rendered-pair
  equality scenarios, plus invalid old-row rejection for SPY and XLK before pruning.
- Independent review approved the repair with no blocking issues and separately ran all ten
  new cases successfully.
- Scoped Ruff, format and strict mypy passed.
- Unchanged local benchmark passed: **92,979,200 bytes peak RSS increase**, 2,104 ms CPU,
  2,249 ms wall, 22 requests and all 11 x 10,000 rows.
- Final full-suite rerun: **RUNNING**, `.tmp/step5-evidence/pytest-retention-final.log`.

## Production qualification

The repaired commit's Ubuntu benchmark and five successful live probes remain pending.
No first-failure result, historical Step 0 run or local benchmark is counted as live success.
Step 4 remains `WAIVED / NOT_EXECUTED`; Step 6 remains separate and inactive.
