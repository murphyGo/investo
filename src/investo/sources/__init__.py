"""Source Adapters — plugin layer.

This package will host one module per data source plus a small set of
internal helpers (`_window`, `_retry`, `_sanitize`, `_registry`,
`protocol`, `aggregator`). Public re-exports are added here as the
adapters and aggregator land in subsequent code-generation steps.
"""
