# u146 Trusted Curated Image Supply Workbench

## Status

Code generation is complete (2026-08-03). The curated library contains 19
filed assets, zero deferred assets, and zero orphans: 15 exact-byte-sealed
legacy entries plus four complete evidence-backed filing graphs.

## Delivered trust boundary

```text
saved exact Commons imageinfo + Structured Data + selected binary
  -> offline reproducible READY_FOR_REVIEW evidence
  -> separate subject/restriction/endorsement review
  -> approved decision with exact evidence/binary/manifest hashes
  -> registry reachability
  -> blocking CI graph verification
```

The workbench cannot fetch, approve, file, write a manifest/registry row, or
touch U137 clearances. It writes a deterministic pending packet only outside
the repository. The committed verifier hashes stored bytes rather than parsed
or reserialized JSON, so whitespace-only evidence changes invalidate approval.

## Filed coverage

- `topic:data-center` — data-center roof/cooling infrastructure.
- `asset:gold` — generic gold bullion.
- `topic:bitcoin-mining` — Bitcoin mining hardware selected only by explicit
  mining, ASIC, or hashrate language.
- `topic:clean-energy` — solar, wind, and grid infrastructure.

An otherwise license-clean semiconductor candidate remained unfiled because
its exact Commons binary URL triggered the unchanged R13 secret-shape guard.
The KOSPI-history candidate also remained unfiled: its thumbnail response had
no provider checksum and the 2055px original exceeded the unchanged dimension
gate. No snapshot field, binary gate, or global redaction policy was weakened.

## Quality evidence

- Curated gate: `19 filed, 0 deferred`; rights graph `15 legacy, 4 evidence-backed`.
- Focused selection/supply/injection/checker/image-selection/replay scope: 115
  tests passed.
- Ruff lint and format check: green on changed source, scripts, and tests.
- Mypy strict: green on the four changed source modules.
- No-paid guard and `git diff --check`: green.
- Fixed 11-date/33-segment archive replay: 33 present, 32 semantic selections,
  one intended abstention, no person portrait selection, and no narrow mining
  asset selected for generic Bitcoin stories.
- Two independent reviews found no remaining Critical/High/Medium issue after
  binary anchoring, closed artifact shapes, strict statement ranks, symlink
  rejection, full JSON/URL secret scanning, and semantic-fit findings were fixed.

## Follow-up

The bounded u147 selection contract is complete. The replay is semantically
stable but remains concentrated in the only available KOSPI and Wall Street
assets. Future supply should add interchangeable, equally current-safe variants
for those keys through this same evidence graph.
