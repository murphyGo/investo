# Step 6.8 Hypothesis Properties

**Unit**: u144 public-document-finalization-contract
**Completed**: 2026-07-22

Four production-boundary properties now run with 100 generated examples each:

1. arbitrary combinations of canonical phrase and pattern-only forbidden
   labels close under public projection and remain idempotent;
2. generated chart/visual supplements with optional artifact IDs always emit
   one ordered, balanced marker pair;
3. arbitrary forbidden-label combinations in the required watchpoint block
   produce byte-identical fallback Markdown/outcomes on repeated fresh runs;
4. arbitrary ordering and duplication of owned findings always yields layout
   region order and sorted unique issue codes.

Strategies generate only domain-valid identifiers and nonblank Markdown, so
failures exercise finalization behavior rather than constructor preconditions.
No example database artifact is tracked.

## Validation

- containment/property module: 17 passed, including 400 generated examples;
- scoped Ruff and format check: passed.
