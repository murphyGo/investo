# Step 3.7 Whole-Chain Watchpoint Incident Regression

**Unit**: u144 public-document-finalization-contract
**Completed**: 2026-07-21

The redacted run `29707052598` watchpoint fixture now crosses the complete
available E2 lifecycle: generated crypto briefing, production reader assembly,
terminal public projection, phase-only repair/validation test collaborators,
and the real skeleton seal. The legacy fixture output still reproduces
`public_diagnostic.raw_label`, while the sealed output reports zero instances
through the existing surface scanner.

The regression pins the two defense layers independently:

- production assembly must replace the legacy unsafe implication before the
  draft leaves `assembled`;
- a typed legacy assembled layout containing the fixture's raw watchpoint body
  is detected by both the owned-region leakage traversal and the existing
  surface scanner before `_project_assembled_draft()`, then returns zero
  findings from both after the real projection handler.

The test-only repair and validate collaborators do not implement Step 4
containment. They only advance the declared phases, invoke the already-landed
read-only scanners/entity adapter, construct the required notification DTO,
and allow the existing seal factory to prove final byte ownership.

## Validation

- fixture whole-chain and independent projection regressions: 2 passed;
- related incident/projection/lifecycle/surface set: 92 passed;
- publisher and orchestrator regression set before final documentation: 1,031
  passed;
- module-boundary, u144 architecture, and publish-validator set: 30 passed;
- Ruff check and format check: passed;
- scoped diff check: passed;
- fresh-eyes review: one missing independent projection source-connection
  assertion corrected; re-review approved with no remaining finding.
