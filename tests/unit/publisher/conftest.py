"""Shared fixtures for u3 publisher tests.

Currently a placeholder; per-test fixtures (e.g., ``tmp_path``-based
``ARCHIVE_ROOT`` redirection — Step 5.3 design decision (a)) are
introduced as the writer + git_ops tests land.
"""
